(async () => {
    /**
     * 1) traverse every json file in `translates/en` folder
     * 2) gather all keys from parsed json object
     * 3) generate appropriate error-codes .js file in `error-codes` folder
     * 4) file name generate from json file name through taking whole name and omitting `-translates.en.json` at the end
     */
})();